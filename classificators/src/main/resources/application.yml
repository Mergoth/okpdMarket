spring:
  data:
    mongodb:
      host: mongodb
      database: okpd
      port: 27017
  profiles:
    active: debug
eureka:
  client:
    register-with-eureka: false
    eureka.client.fetch-registry: false


server:
  context-path: /classificators
  port: 8080

