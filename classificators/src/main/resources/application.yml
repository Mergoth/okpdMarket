spring:
  data:
    mongodb:
      host: localhost
      database: okpd
      port: 27017
      username: user
      password: 123
  profiles:
    active: debug
eureka:
  client:
    register-with-eureka: false
    fetch-registry: true
server:
  context-path: /classificators
  port: 8080

