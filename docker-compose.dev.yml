version: '3'
services:
  classificators:
    build: classificators
  config:
    build: config
    environment:
      - SPRING_PROFILES_ACTIVE=local
  registry:
    build: registry
  monitoring:
    build: monitoring
  gateway:
    build: gateway
  classificators-mongodb:
    build: classificators-mongodb